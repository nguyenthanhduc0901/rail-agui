"use client";

export const useRailDashboardContext = () => {
  // Intentionally no-op: the app no longer injects rail data context from frontend.
  // The agent must fetch task-specific data via tools.
};